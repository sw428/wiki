# 03_カンプとブラウザで測る場所をそろえる

デザインツールのテキストレイヤーが21pxだからといって、ブラウザ上の背景帯全体も21pxになるとは限らない。比較する対象をそろえてからCSSへ置き換える。

## テキストレイヤーの高さを親の高さへ直訳しない

CSSへ移すときは、次を分けて確認する。

1. `font-size`。
2. `line-height`。
3. フォントの字形とメトリクス。
4. 親要素の `padding`。
5. 背景や罫線を持つ要素の実寸。

```css
.notice-band {
  font-size: 14px;
  line-height: 1.6;
  padding-block: 3px;
  background-color: #006ea8;
}
```

テキストレイヤーの高さと、背景を持つ親要素の高さは別に測る。まず行の高さと親の内側余白から組み立てる。`height`固定は、部品全体の高さが要件として決まっている場合に使う。

`margin-top: -1px`のような微調整は、通常配置、境界線、端数丸め、フォントの違いを確認した後に検討する。

## 選択枠と見えている字形を分ける

デザインツールでテキストレイヤーを選択したときの領域は、レイヤーのバウンディングボックスである。文字や記号として見える線は字形（グリフ）であり、選択枠全体と同じ幅・高さではない。

```text
テキストレイヤーのバウンディングボックス
┌──────────┐
│    >     │ ← 見えている字形
└──────────┘
```

| 観測対象 | 分かること |
| --- | --- |
| デザインツールの選択枠 | レイヤーが持つ範囲 |
| 字形 | 画面上で実際に見える文字の形 |
| ブラウザの要素ボックス | CSSレイアウトで使われるcontent・padding・borderを含む箱 |

隣接する文字が別レイヤーなら、まずレイヤー間の距離を測る。一つのテキストレイヤー内に記号と文字が含まれる場合は、必要に応じて一時的なレイヤー分割や長方形の重ね合わせで近似する。

カンプの値を仮置きした後、同じ画面幅のブラウザ表示と比較して調整する。

フォント自体の違いが疑わしい場合は、[ブラウザで実際に使われたフォントを確認する](./04_ブラウザで実際に使われたフォントを確認する.md)へ進む。ブラウザ側の寸法を確認する場合は、[DevToolsで計算後の寸法を確認する](./05_DevToolsで計算後の寸法を確認する.md)を使う。

## 関連ページ

- [インラインと行の仕組み](../08_インラインと行の仕組み/01_文字の行間と高さを確認する.md)
