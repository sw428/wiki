# 02_line-heightの指定値と継承

親と子で文字サイズが違うとき、`line-height: 1.5`と `150%`は同じ結果になるとは限らない。親で何が計算され、どの値が子へ渡るかを分ける。

基本の行高計算は[文字の行間と高さ](./01_文字の行間と高さを確認する.md)へ戻る。

## 単位なしとパーセントでは継承される値が違う

同じ要素へ指定した直後は、`1.5`と `150%`のどちらも、その要素の文字サイズの1.5倍になる。違いは子孫へ渡る値である。

| 指定 | その要素での計算 | 子孫へ継承されるもの |
| --- | --- | --- |
| `line-height: 1.5` | 使用する文字サイズ × 1.5 | 数値 `1.5` |
| `line-height: 150%` | その要素の計算済み文字サイズ × 150% | 計算後の長さ |
| `line-height: 1.5em` | その要素の計算済み文字サイズ × 1.5 | 計算後の長さ |

```css
.parent {
  font-size: 20px;
  line-height: 1.5;
}

.child {
  font-size: 12px;
}
```

単位なしの `1.5`を継承した子では、子の文字サイズ12pxを基準にして18px相当の行高になる。

親を `line-height: 150%`にすると、親で計算された30px相当の長さが子へ渡る。子の文字サイズを12pxへ変えても、子の文字サイズを基準に1.5倍として計算し直すわけではない。

子孫で文字サイズが変わる本文範囲では、単位なしの数値を基本にすると扱いやすい。ただし、これはCSS全体に共通する継承規則ではなく、`line-height`固有の計算方法である。

## CSSは要素へ指定され、文字の描画へ使われる

文字そのものは[テキストノード](../../../05_参照/テキストノードとCSS適用境界.md)として存在する。CSSは基本的に要素へ指定され、継承できる値がDOMの親子関係を通って子孫へ渡り、文字の描画に使われる。

| 段階 | 起きていること | 例 |
| --- | --- | --- |
| CSSの指定先 | 宣言は要素に対して解釈される | `.lead { color: red; }` |
| 継承と計算 | DOMの親子関係を通って値が伝わる | `font-size`、`line-height` |
| 描画 | 計算された値が文字や行の描画に使われる | 字形、フォント、行の高さ |

`font-family`、`font-weight`、`color`、`letter-spacing`なども継承する代表例である。すべてのCSSプロパティが継承するわけではないため、対象のプロパティごとに確認する。

## 仕様確認先

- [CSS Inline Layout Module Level 3 - `line-height`](https://drafts.csswg.org/css-inline-3/#line-height-property)
- [CSS Cascading and Inheritance - Inheritance](https://www.w3.org/TR/css-cascade-5/#inheriting)
