# 01_文字の行間と高さを確認する

ブラウザが文字を並べるために作る箱から確認する場合は、前提知識の[インラインブロックとインラインボックスの違い](../../../01_前提知識/01_HTML・CSS/08_インラインと行の仕組み/02_インラインブロックとインラインボックスの違い.md)へ戻る。

文章の高さがカンプと合わないときは、要素へすぐ固定高を付けず、文字サイズ・一行の高さ・行数を分けて確認する。

このページでは、`font-size`と `line-height`から一行の高さを決め、文章の折り返しによって要素全体の高さが生まれるところまでを扱う。

## 一行の高さを指定する

```html
<p class="lead">制作から公開後の更新まで支援します。</p>
```

```css
.lead {
  font-size: 16px;
  line-height: 1.5;
}
```

単位なしの `1.5`は、文字サイズの1.5倍を一行の高さとして使う。この例では一行が24px相当になる。

```text
16px × 1.5 = 24px
```

`line-height`は文字そのものの高さを変える指定ではない。文字を配置する行の高さへ影響する。

## カンプのpx値を倍率へ直す

デザインツールで行の高さがpxで示されている場合は、`line-height ÷ font-size`で倍率を確認できる。

```text
21 ÷ 14.4 = 1.458333... → 約1.46
48 ÷ 32 = 1.5
24 ÷ 16 = 1.5
```

```css
.lead {
  font-size: 14.4px;
  line-height: 1.46;
}
```

カンプへ細かく寄せる必要があれば計算値に近い倍率を使う。複数箇所を同じリズムで管理したい場合は、表示を確認したうえで `1.5`のような扱いやすい値へそろえる選択肢もある。

## 行数から結果として生じる高さ

単純なテキストだけを持つブロックが `height: auto`で、paddingやborderもない場合、内容の高さは行の積み重なりから生じる。

```text
4行 × line-height 21px = 84px
```

この84pxは、親へ `height: 84px`を指定した結果とは限らない。固定高を追加する前に、次を確認する。

1. 行数は `<br>`による改行か、画面幅による自然な折り返しか。
2. `line-height`はpx指定か、文字サイズから計算する倍率か。
3. 親にpaddingやborderがあるか。
4. 行内に画像など、行を高くする別の要素があるか。

`white-space: nowrap`は自然な折り返しを止めるが、`<br>`の強制改行まで消す指定ではない。特定の画面幅で自然に折り返しているだけなら、固定高や `<br>`を増やさず、中間幅でも確認する。

## ブロックの後ろのリンクが次の行に来る理由

次の `p`と `a`は兄弟要素である。

```html
<div class="staff-card__body">
  <p class="staff-card__text">インタビューの概要です。</p>
  <a class="staff-card__link" href="./staff/detail.html">続きを読む</a>
</div>
```

ブラウザの既定表示に近い状態では、`p`はブロックボックスを作る。後続の `a`が次の行から表示されるのは、`a`自身が改行を作るためではなく、直前の `p`が独立したブロックとして終了しているためである。

```text
インタビューの概要です。
続きを読む
```

親をFlexやGridにした場合や、`p`の `display`を変えた場合は並び方も変わる。HTML上の親子・兄弟関係と、CSS上の並び方を分けて確認する。

## 次に確認すること

- 子要素で文字サイズが変わる：[line-heightの指定値と継承](./02_line-heightの指定値と継承.md)
- 行内のボタン箱を作る：[inline-blockとinline-flex](./03_inline-blockとinline-flexを使い分ける.md)
- 画像の下や文字の縦位置がずれる：[ベースラインと見た目のずれ](./04_ベースラインと見た目のずれ.md)
- 改行やインデントが間隔に見える：[HTMLの空白と疑似要素](./05_HTMLの空白と疑似要素を整える.md)
- 文字と箱のどちらを中央へ置くか迷う：[文字と要素の箱を中央に寄せる](./06_文字と要素の箱を中央に寄せる.md)

次へ進む目安は、文字サイズ、一行の高さ、行数、要素全体の高さを分けて確認できること。
