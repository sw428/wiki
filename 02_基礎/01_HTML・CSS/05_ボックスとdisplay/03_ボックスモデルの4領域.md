# ボックスモデルの4領域

画面上の箱を、`content / padding / border / margin` の4領域に分けて確認する。

## 箱の内側と外側を分ける

```text
margin
└─ border
   └─ padding
      └─ content
```

| 領域 | 何がある場所か |
| --- | --- |
| `content` | 文字や画像などの内容 |
| `padding` | 内容と枠線の間 |
| `border` | 箱の枠線 |
| `margin` | ほかの箱との外側の間隔 |

この4領域で箱の寸法を読む考え方がボックスモデルである。

## 見えている広さを全部「余白」と決めつけない

要素が中身より広く見えても、必ずしも `margin` や `padding` が原因とは限らない。

横方向では、まず `display`、親の幅や列幅、自分の `width` を確認し、その後で `padding / border / margin` のどこが広いかを見る。

縦方向では、文字が複数行になっていないか、子要素の高さが積み重なっていないか、`line-height`が大きくないかを確認してから、`padding / border / margin`を見る。

見た目だけで余白の種類を決めず、対象の要素をDevToolsで選んで確認する。

## DevToolsで4領域を対応させる

DevToolsのボックスモデル図では、選択した要素の `content / padding / border / margin` を数値と領域で確認できる。

![DevToolsのボックスモデル図でcontent、padding、border、marginを確認する](../img/DevTools_ボックスモデル.png)

CSSに書いた値だけで判断せず、どの領域が実際の大きさを作っているかを図と画面上のハイライトで対応させる。

`width`や`height`が箱のどこまでを含むかは、[box-sizingでwidthの範囲を決める](./04_box-sizingでwidthの範囲を決める.md)へ進む。
