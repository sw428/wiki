# 画像と動画の表示をやさしく理解する

> 試し版。貼ってもらった「10_メディア設計」の全論点を、具体例から説明し直したものです。元のノートを置き換える正本ではありません。

このページで扱うのは、**画像や動画を、思った大きさ・形・位置で表示する方法**です。

たとえば、こんな困りごとを扱います。

- 写真を横幅いっぱいにしたい。でも縦につぶしたくない。
- 大きさの違う写真を、同じ形のカードにそろえたい。
- スマホだけ違う写真にしたい。
- 写真が出た瞬間、下の文章がガクッと動くのを防ぎたい。

「レスポンシブ」は、画面や置き場所の幅が変わっても、それに合わせて表示すること。スマホ向け表示を、元のノートでは「SP」と呼んでいます。

まず1〜5で画像1枚の扱いを説明します。その後に、ロゴ、円形写真、地図、動画などへ広げます。細かい話も残していますが、一度に全部理解する必要はありません。止まったら「何番の、この文が分からない」と指せる形にしています。

コード内の `dog.jpg` などは説明用のファイル名です。実際に試す場合は自分の画像へ置き換えます。数値は例であり、案件の指定値ではありません。別の節にあるCSSは、それぞれ独立した例として読んでください。

## 1 画像1枚をそのまま表示する

横600、縦400ピクセルの犬の写真があるとします。

まずHTMLに、表示したい画像を書きます。

```html
<div class="photo-area">
  <img
    class="photo"
    src="dog.jpg"
    alt="芝生に座っている犬"
    width="600"
    height="400"
  >
</div>
```

この例の `div` は、写真を置く場所です。その中に `img` が入っています。

```text
div.photo-area  ← 外側の要素。このimgの「親」
└─ img.photo   ← 中にある要素。このdivの「子」
```

「親に指定する」と言われたら、この例では `.photo-area` にCSSを書きます。「画像に指定する」なら `.photo` です。

HTMLの各部分は、次の意味です。

| 書いてあるもの | この例での役割 |
|---|---|
| `src="dog.jpg"` | この写真を読み込む |
| `alt="芝生に座っている犬"` | 画像の代わりに内容を伝える文 |
| `class="photo"` | CSSからこの画像を選ぶための名前 |
| `width="600" height="400"` | 幅と高さの情報を先に伝える。横と縦の割合も分かる |

次にCSSを書きます。

```css
.photo-area {
  width: 300px;
}

.photo {
  display: block;
  width: 100%;
  height: auto;
}
```

このとき、画面では **横300px、縦200px** になります。

1. 外側の `.photo-area` が、横300pxになる。
2. 画像の `width: 100%` は、この例では「親の横幅と同じにする」。だから横300pxになる。
3. `height: auto` は「高さは自動で決める」。この画像では、元の横600・縦400の割合を保って縦200pxになる。

親を横150pxにすれば、写真は横150・縦100pxになります。写真全体が同じ割合で小さくなるので、犬の顔が横につぶれません。

`display: block` は、写真を文字の行に混ぜる扱いから、独立した箱の扱いに変えます。文字の足元に合わせるための隙間が、画像の下に出るのを避けやすくなります。

**ここでは「幅を決め、高さは写真の割合に任せる」と読めれば十分です。**

## 2 同じ幅と高さでも何の数値かが違う

さっきの例には、600、400、300、200という数が出てきました。数字が違っても矛盾ではありません。

| 何を見ているか | 例 | 簡単に言うと |
|---|---|---|
| 画像ファイルのピクセル寸法 | 横600・縦400 | ファイルの中にある画像データの大きさ |
| HTMLの寸法属性 | `width="600" height="400"` | HTMLに書いてブラウザへ伝えた幅と高さ |
| 画面上の表示寸法 | 横300px・縦200px | CSSなどを計算した結果、画面で使っている大きさ |
| アスペクト比 | `3 / 2` | 横と縦の割合。ここでは横3に対して縦2 |

画像ファイルのピクセルと、CSSで指定するpxは、同じものを数えているとは限りません。細かい点をたくさん持つ画像を、小さな表示領域へ収めることもできます。高密度画像は15で説明します。

「解像度」という言葉も、画像のピクセル数、PPI、画面の密度などを指すことがあります。PPIは1インチあたりの画素数です。混ざりそうなら、まず「ファイルの横と縦」か「画面での横と縦」かを言い分けます。

### HTMLに800と書いても表示が800px固定とは限らない

```html
<img class="photo" src="landscape.jpg" alt="山の風景" width="800" height="450">
```

ここへ `width: 100%; height: auto;` を当て、親の幅を600pxにすると、他の制約がなければ表示は横600・縦337.5pxです。

```text
高さ = 表示する幅 × 元の高さ ÷ 元の幅
     = 600 × 450 ÷ 800
     = 337.5px
```

HTMLの寸法は、CSSで別の寸法を指定しなければ表示サイズの基準にもなります。CSSで幅を変えたからといって、HTMLの数値が間違いになるわけではありません。

HTMLには実画像と合う縦横比を書きます。エディタが自動入力した値も確認します。比率が違うと、読み込み前に用意した高さと、読み込み後の高さが食い違う原因になります。

## 3 全体を見せるか決まった形に収めるか

横600・縦400の写真を、そのまま縮めると横長のままです。

でも「スタッフの写真を全部、正方形にそろえたい」こともあります。ここで、**写真本来の形**と**画面に用意する形**が違ってきます。

たとえるなら、写真をそのまま貼るか、決まった形の額に入れるかの違いです。ここでいう「枠」は表示する領域のことで、線を描く `border` が必要という意味ではありません。

| やりたいこと | 最初に考える指定 | 画面でどうなるか |
|---|---|---|
| 写真全体を自然な形で見せる | 幅を決めて `height: auto` | 写真の縦横比に合わせて高さが変わる |
| 正方形など決まった形を写真で埋める | 枠を作って `object-fit: cover` | 写真の一部が切れることがある |
| 決まった形の中に写真全体を入れる | 枠を作って `object-fit: contain` | 枠の中に余りが出ることがある |

### coverとcontainを同じ写真で比べる

横600・縦400の写真を、横300・縦300の正方形へ入れます。

**`cover` の場合:** 写真を横450・縦300相当まで拡大縮小します。正方形を全部埋めますが、横150px分が入りません。中央配置なら、左右75px相当ずつが見えなくなります。

**`contain` の場合:** 写真を横300・縦200相当にします。写真全体が入りますが、縦100px分が余ります。中央配置なら、上下50px相当ずつが空きます。

この「空き」は `margin` や `padding` を追加した結果ではありません。画像を表示する箱の中で、写真が覆っていない部分です。

切れるのは画面での表示です。元の画像ファイルを書き換えたり、削ったりするわけではありません。

### object-fitだけでは枠は作れない

`object-fit` が決めるのは、**すでに大きさが決まった画像要素の中へ、画像データをどう収めるか**です。

`object-fit: cover` だけを付けても、画像が自動で正方形になるわけではありません。先に表示する箱の幅と高さを決める必要があります。

`height: auto` で画像本来の比率どおりに表示していれば、箱と写真の割合が同じなので、`object-fit` を変えても違いが見えにくくなります。

仕組みの確認先: [CSS Imagesのobject-fit](https://drafts.csswg.org/css-images-3/#the-object-fit)。

## 4 aspect-ratioは大きさではなく形を決める

`aspect-ratio` は、横と縦の割合をCSSで指定するものです。

```css
.frame {
  width: 300px;
  aspect-ratio: 3 / 2;
}
```

この空の箱は、他の制約がなければ横300・縦200pxになります。

- 横300pxを決めたのは `width`。
- 「横3に対して縦2」という形を決めたのが `aspect-ratio`。
- その2つから、高さ200pxが計算される。

`aspect-ratio: 1` は `1 / 1` と同じで、正方形です。

### 数字を半分にしても箱は半分にならない

次の3つは、どれも同じ横長の形です。

```css
aspect-ratio: 1600 / 900;
aspect-ratio: 800 / 450;
aspect-ratio: 16 / 9;
```

幅が800pxなら、どれも高さ450pxになります。小さくしたいなら、比率の数字ではなく `width` などを変えます。

### 大きくなりすぎるのを止めるのはmax-width

```css
.card-media {
  width: 100%;
  max-width: 570px;
  aspect-ratio: 570 / 416;
}
```

これは「親の幅に合わせる。ただし570pxより大きくしない。その幅に合わせて高さを決める」です。

| 親の幅 | この箱の幅 | この箱の高さ |
|---|---:|---:|
| 800px | 570px | 416px |
| 285px | 285px | 208px |

570pxで止めているのは `max-width` です。`aspect-ratio` だけでは止まりません。

### 幅と高さを両方固定したらどうなるか

```css
.frame {
  width: 300px;
  height: 300px;
  aspect-ratio: 16 / 9;
}
```

この場合は幅も高さも指定済みなので、`aspect-ratio` を足しても横長へ直してはくれません。

比率が寸法計算に使われるには、基本的に幅か高さの少なくとも片方が自動計算である必要があります。文字などの中身、最小・最大サイズの制約でも結果は変わり得ます。「何があっても指定比率になる命令」とは考えません。

確認先: [CSS Box Sizingのaspect-ratio](https://drafts.csswg.org/css-sizing-4/#aspect-ratio)。

## 5 枠を画像自身に作るか親に作るか

ここまでの「決まった形の枠」は、2通りの場所に作れます。

### 画像1枚で完結するならimg自身を枠にできる

```html
<img class="square-photo" src="dog.jpg" alt="犬" width="600" height="400">
```

```css
.square-photo {
  display: block;
  width: 300px;
  height: auto;
  aspect-ratio: 1;
  object-fit: cover;
}
```

画像要素そのものを横300・縦300pxにして、その中へ犬の写真を収めます。

この例ではCSSの `aspect-ratio: 1` を表示の形に使います。`height: auto` も明示しているので、HTMLの `height="400"` を画面上の固定高として残しません。

`aspect-ratio: 375 / 400` に変えれば、その割合の縦長になります。画像を囲む `div` は、必ず必要なわけではありません。

### 画像とは別に領域を持ちたいなら親を枠にする

```html
<div class="frame">
  <img class="frame__image" src="dog.jpg" alt="犬" width="600" height="400">
</div>
```

```css
.frame {
  width: 300px;
  aspect-ratio: 1;
  overflow: hidden;
}

.frame__image {
  display: block;
  width: 100%;
  height: 100%;
  object-fit: cover;
}
```

今回は、正方形を作る場所が `.frame` に変わりました。

1. 親の幅300pxと比率1:1から、親の高さ300pxが決まる。
2. 子画像の `width: 100%; height: 100%` で、画像要素も300×300pxになる。
3. その中へ、`cover` で写真を収める。

`height: 100%` は「写真の元の高さを使う」ではなく、この例では「親の高さと同じにする」です。親の高さを、この子の指定が作っているわけではありません。

親の高さが決まっていない別の構造へ `height: 100%` だけを持っていっても、同じ結果になるとは限りません。

### 子をheight: autoのままにすると何が違うか

この例で子画像を `width: 100%; height: auto;` にすると、元の写真の割合から画像要素は横300・縦200pxになります。

親は300×300px、画像は300×200pxなので、下に100px分の空きができます。親を作っただけでは、画像がその高さへ伸びるとは限らないのです。

### 親の枠が役に立つ場面

- 写真の上へ、文字、バッジ、暗い膜、ボタンを重ねたい。
- 読み込み中の仮表示も、同じ場所に置きたい。
- 画像がない・非表示・読み込み失敗でも、表示領域を残したい。
- クリックできる範囲を、写真とは別に管理したい。
- 外側の配置や形と、中の写真の見せ方を別々に変えたい。

親そのものを `display: none` にすれば、もちろん領域も消えます。「画像がなくても残る」は、親の枠が表示されていることが前提です。

画像を包む要素を「ラッパー」と呼ぶことがあります。意味は、ここで使った外側の `div` のような要素です。

たとえば `article.staff-card` は「紹介カード1件」を表し、内側の `.staff-card__media` は「画像を表示する場所」を担当します。カード全体を表す箱と、写真用の箱は目的が違います。

単独の画像に幅・比率・角丸を指定するだけで済むなら、追加の箱は省けます。円形や比率指定があるという理由だけで、必ずラッパーを作るわけではありません。

## 6 外・箱・中身はどのコードのことか

元のノートの「外・箱・中身」を、カード一覧に置き換えます。

```text
カード一覧                 ← カードを横に何枚並べるか
└─ カード
   ├─ 写真を置く領域       ← 横長か、正方形か
   │  └─ 写真              ← 全部見せるか、切って埋めるか
   └─ 見出しや文章
```

| 困っていること | 主に見る場所 | 指定の例 |
|---|---|---|
| カードを3列にしたい、間を空けたい | 一覧の親 | `grid` / `flex` / `gap` |
| 写真の領域を正方形にしたい | 写真の表示領域 | `width` / `aspect-ratio` |
| 枠は合っているが顔が切れる | 画像の中身 | `object-fit` / `object-position` |

「責務を分ける」は、この**どの指定が何を担当するかを分ける**という意味です。

写真の切れ方が気になるのに一覧の列幅まで動かすと、関係のない部分も変わります。まず、直したいものに対応する行だけを見ます。

## 7 画像が出た瞬間に文章が動くのを防ぐ

画像の読み込みには時間がかかります。その前に画像の高さが分からないと、こんな順で表示されることがあります。

```text
読み込み前                 読み込み後
見出し                     見出し
本文                       写真が表示される
                           本文が下へ押される
```

このような予期しない位置の変化を評価する指標が **CLS** です。画像が原因の移動を減らすには、読み込む前から「ここに写真が入る」と場所を用意します。

`width="600" height="400"` があると、ブラウザは画像データが届く前でも3:2の比率を知れます。表示幅が300pxなら、高さ200px分を先に用意できます。

「比率予約」は、この**先に高さを計算できる情報を渡すこと**です。

| 情報の渡し方 | いつ使えるか | 向いている用途 |
|---|---|---|
| HTMLの `width` / `height` | HTMLを読み取った段階で情報を得られる | 画像本来の比率を先に伝える |
| CSSの `aspect-ratio` | CSSが読み込まれ、その要素へ適用された後 | デザインで決めた枠を先に作る |
| どちらもない | 他の寸法指定もなければ、画像の寸法情報を得るまで高さが分からないことがある | 後から位置が動く原因になりやすい |

CSSでも、画像の読み込み完了より前に枠を作れます。ただし、HTMLとCSSの情報が使えるタイミングは同じとは限りません。

HTMLに画像の寸法を書き、CSSで表示幅を調整するのが、元のノートの基本方針です。違う形の枠が必要ならCSSの比率も加えます。同じ比率を理由なく何か所にも書く必要はありません。

これで画像由来のズレを減らせますが、フォントや後から追加される広告など、別の原因のズレまで全部なくなるわけではありません。

確認先: [web.devの画像寸法とCLS対策](https://web.dev/articles/optimize-cls)。

## 8 カンプから表示方法を選ぶ

「カンプ」は、完成時の見た目を示したデザイン見本のことです。

見本に合わせるときは、まず元の画像と見本を比べます。

1. 元画像の横と縦を確認する。
2. カンプで画像が見えている領域の横と縦を測る。
3. 元画像のどこかが切れているかを見る。
4. PCとスマホの見本に合わせたあと、その間の幅でも表示を確認する。

横600・縦400の写真が、見本では横300・縦200になっていて、どこも切れていないなら、自然な比率で縮める方法から試せます。

同じ写真が横300・縦300になっていて左右が切れているなら、正方形の枠と `cover` が候補です。枠を画像自身に作るか親に作るかは、5で説明した「画像以外も同じ領域で管理するか」で選びます。

### 1枚のカンプでは高さの決め方を断定できない

横375・縦400の領域は、基準幅では次のどちらでも作れます。

```css
/* 高さ400pxを保つ */
.hero-a {
  height: 400px;
}

/* 横375に対して縦400という形を保つ */
.hero-b {
  aspect-ratio: 375 / 400;
}
```

この2つは、幅が300pxになったときに違いが出ます。

- 高さ固定なら、幅が変わっても高さ400px。
- 比率指定なら、高さは `300 × 400 ÷ 375 = 320px`。

写真の形を保ちたいのか、ページ先頭の領域の高さや次の文章の開始位置を優先するのかで判断が変わります。`min-height` は「最低この高さ」という指定で、中身に応じてさらに高くできる点が `height` と違います。

別の幅のカンプから変化を読むか、作った人に確認します。確認できなければ「今回は比率を優先した仮の実装」と理由を残し、他の幅で写真の構図と画面を占める広さを見ます。

元のノートに挙がる [Shopify Dawn](https://github.com/Shopify/dawn/blob/main/sections/image-banner.liquid) には画像に合わせる高さと段階別の高さ設定があり、[WordPressのCoverブロック](https://wordpress.org/documentation/article/cover-block/) にも比率と最小高さの設定があります。どちらの考え方も使われる例ですが、目の前のカンプの意図までは分かりません。

画像ファイルが単に2倍のピクセル数で書き出されているだけなら、表示の形を変える話ではありません。これは15で分けます。

## 9 PCとスマホで画像を変える

横長の写真をスマホの縦長画面へ入れると、左右が大きく切れることがあります。顔や文字が消えるなら、CSSの切り抜きだけで頑張らず、スマホ用の別画像を用意する方法があります。

`picture` は、条件に応じて画像の候補を選ぶために使います。

```html
<picture>
  <source
    media="(min-width: 768px)"
    srcset="mv-pc.jpg"
    width="1920"
    height="500"
  >
  <img
    class="mv-image"
    src="mv-sp.jpg"
    alt="サービスの紹介"
    width="375"
    height="400"
  >
</picture>
```

```css
.mv-image {
  display: block;
  width: 100%;
  height: auto;
}
```

この例では、画面幅768px以上ならPC画像、それより狭ければスマホ画像が選ばれます。768pxは、この例で決めた切り替え位置です。

| HTML | 担当していること |
|---|---|
| `picture` | この中の候補から画像を選ぶためのまとまり |
| `source` | 「この条件ならこの画像」という候補を渡す |
| `img` | 選ばれた画像を実際に画面へ表示する |

`source` 自身がもう1枚の画像として表示されるわけではありません。表示サイズを調整するクラスや `alt` は、この例では `img` に置きます。

選ばれた `source` に有効な `width` / `height` があれば、それらが `img` の寸法・比率の計算に使われます。PC画像の1920×500と、スマホ画像の375×400をそれぞれ先に伝えることで、読み込み前にも選択候補に合った領域を用意しやすくなります。

ただし、1920と書いても、画面で必ず横1920pxになるわけではありません。上のCSSなら、実際の横幅は置き場所の幅に合わせます。

確認先: [HTML Standardの画像の寸法計算](https://html.spec.whatwg.org/multipage/rendering.html#attributes-for-embedded-content-and-images)。

### max-widthとwidthを間違えると画像が小さいままになる

この2つは、似ていますが別の指定です。

| 指定 | この例での意味 |
|---|---|
| `width: 100%` | 親の利用できる横幅に合わせる |
| `max-width: 100%` | 親の横幅を超えないようにする。小さい画像を広げる指定ではない |

たとえば、幅600pxの親に、幅300pxを基準に表示される画像が入っているとします。

- `max-width: 100%` だけなら、300pxは上限600px以内なので、そのままでよい。
- `width: 100%` なら、600pxまで広げる。

親が200pxに狭くなれば、前者も上限に合わせて縮められます。つまり `max-width` だけでも「大きすぎる画像を縮める」ことはできます。

### PC画像が選ばれたのに幅375pxのままになる例

先ほどの `source` から寸法属性を省き、`img` には `width="375" height="400"` を残したとします。CSSも `max-width: 100%; height: auto;` だけにします。

他のレイアウト制約がなく、親の幅が十分ある場合、PC画像が選ばれても、HTMLの375が幅の基準として残ることがあります。

読み込んだPC画像が1920×500なら、表示の高さは次のようになります。

```text
表示幅375px × PC画像の高さ500 ÷ 幅1920
= 約97.66px
```

「PC画像を選べているか」と「画像の表示幅が十分あるか」は、別々に確認する必要があります。親幅いっぱいにしたい場合は `width: 100%` を指定します。

### 画像の選択と枠の形も別々に決める

画像本来の形をそのまま使うなら、最初の `picture` と `height: auto` の組み合わせで考えます。

一方、「スマホでは375:400、PCでは1920:500の枠にする」とデザイン側で決めるなら、`picture` の外へ親の枠を置きます。

```html
<div class="mv-media">
  <picture>
    <source media="(min-width: 768px)" srcset="mv-pc.jpg" width="1920" height="500">
    <img class="mv-media__image" src="mv-sp.jpg" alt="サービスの紹介" width="375" height="400">
  </picture>
</div>
```

```css
.mv-media {
  aspect-ratio: 375 / 400;
  overflow: hidden;
}

.mv-media__image {
  display: block;
  width: 100%;
  height: 100%;
  object-fit: cover;
}

@media (min-width: 768px) {
  .mv-media {
    aspect-ratio: 1920 / 500;
  }
}
```

この例では `picture` に独自の表示箱を作るCSSを当てていません。画像の割合の高さは、外側の `.mv-media` が作った高さを基準にします。途中の要素に `display: block` などを加えて高さの基準を変える場合は、そこも確認が必要です。

この方式では、表示枠の形を管理しているのはCSSです。`source` の寸法属性を、デザイン上の枠の比率を決める担当にはしません。

幅300pxでスマホ用の比率なら、枠の高さは320pxです。`375 / 400` は「375×400pxに固定」という意味ではありません。

### 1枚の写真で足りるかを先に見る

- 多少切れても伝えたい内容が残るなら、1枚の写真と `cover` で対応できることがある。
- 顔・文字・商品が切れるなら、別の構図を持つPC用／スマホ用画像を検討する。
- 全体を見せることが大切で、空きがあってよいなら `contain` も選べる。

同じ構図・同じ比率で、細かさだけ違う画像を選びたい場合は、15の `srcset` が中心です。画像のピクセル数を2倍にしても、`cover` による切れ方は解決しません。

## 10 ロゴや文字入り画像はまず切らずに見せる

ロゴや「予約はこちら」と書かれた画像は、一部を切ると意味が変わったり、読めなくなったりします。

まず、幅だけを指定して高さを自動にします。

```css
.action-image {
  display: block;
  width: 167px;
  max-width: 100%;
  height: auto;
}
```

これは「通常は幅167px、置き場所がもっと狭ければ縮める、高さは画像の割合に合わせる」です。

SVGのロゴでも考え方は同じです。SVGは図形を記述する画像形式ですが、画面での表示幅まで自動でデザインどおりになるわけではありません。幅が決まらなければ、ロゴ用のCSSで145pxなどの表示幅を指定します。

### 同じロゴをPCでは大きくスマホでは小さくする

```html
<a class="header-logo" href="/">
  <img class="header-logo__image" src="logo.svg" alt="サイト名" width="199" height="25">
</a>
```

```css
.header-logo {
  --logo-width: 358.13px;
  display: block;
  width: min(100%, var(--logo-width));
}

.header-logo__image {
  display: block;
  width: 100%;
  height: auto;
}

@media (max-width: 767px) {
  .header-logo {
    --logo-width: 268.36px;
  }
}
```

`--logo-width` は、値に名前を付けて使う「CSS変数」です。`var(--logo-width)` で、その値を取り出します。

`min(100%, var(--logo-width))` は、親の幅と指定したロゴ幅のうち、小さい方を使います。横に入りきらないのを避けるためです。

PCとスマホで変えるのは表示幅だけ。ロゴの形は199:25の割合を保ちます。HTMLの199×25を、CSSの358.13×何かに毎回書き換える必要はありません。

### 縦と横を両方固定して合わせようとしない

ロゴの幅を変えたら、高さも同じ割合で変わる必要があります。幅と高さを別々に固定して比率がずれると、ロゴが横長や縦長に変形します。

幅と高さを両方固定したあとに `aspect-ratio` を足しても、その2つの数値を直してはくれません。

同じ比率のロゴなら、画像の比率と `height: auto` で足りることが多いです。表示箱も画像と同じ比率になるので、`object-fit` も通常は不要です。固定幅・固定高の箱が必要なら、全体を入れる `contain` を検討します。

PCとスマホで比率の違うロゴを使う場合は、9の画像切り替えと表示枠の判断に戻ります。

### viewBoxは画面での大きさではない

SVGの `viewBox` は、**SVG内部のどの座標範囲を表示に使うか**を表します。CSSの表示幅そのものではありません。

表示を少し小さくしたいからといって `viewBox` を変えると、図形の見える範囲や余白まで変わることがあります。表示サイズの調整は、まずCSSの `width` で行います。

カンプと数pxほど合わないときは、次の順で見ます。

1. 測定値の小数が丸められていないか。
2. 画像の領域に透明な余白があり、見える図形より広くなっていないか。
3. SVGの `viewBox` と素材の縦横比がどうなっているか。
4. 厳密な一致が必要なら、素材とカンプのどちらに合わせる条件なのか。

無理に変形や切り抜きで差を隠さず、必要なら素材やデザインを直す範囲として確認します。

確認先: [SVG 2のviewBox](https://svgwg.org/svg2-draft/coords.html#ViewBoxAttribute)。

## 11 円形の写真と外側のリングを分ける

写真を丸くするだけなら、正方形の画像要素に `border-radius: 50%` を付ける方法があります。

外側に色の付いたリングもあり、写真の切れ方を別々に調整したい場合は、3つに分けると分かりやすくなります。

```text
外側の円：色の付いた背景
└─ 内側の円：写真を見せてよい範囲
   └─ 写真：その範囲に収める画像
```

```html
<div class="avatar__media">
  <div class="avatar__frame">
    <img class="avatar__image" src="person.jpg" alt="紹介する人物" width="600" height="400">
  </div>
</div>
```

```css
.avatar__media {
  --avatar-size: 192px;
  --ring-width: 8px;
  width: var(--avatar-size);
  aspect-ratio: 1;
  padding: var(--ring-width);
  border-radius: 50%;
  background: #f1f1f1;
  box-sizing: border-box;
}

.avatar__frame {
  width: 100%;
  height: 100%;
  border-radius: 50%;
  overflow: hidden;
}

.avatar__image {
  display: block;
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center;
}
```

この例は、外側192px、リングの太さ8pxです。内側の写真の直径は `192 − 8 − 8 = 176px` です。色の付いた背景が、写真の周りに8pxずつ見えます。

`box-sizing: border-box` は、「指定した192pxの中にpaddingも含める」という設定です。初期値の `content-box` のままだと、指定した幅の外へpaddingが足され、外周が大きくなります。ここではborderはありません。

元のコードの `12rem` / `0.5rem` は、ルートの文字サイズが16pxなら192px / 8pxです。この試し版では寸法の関係を追いやすいようpxへ置き換えています。

### 何を変えたいかによって触る場所を変える

| 変えたいこと | 触る指定 |
|---|---|
| 円全体を大きくする | 外側の `--avatar-size` |
| 周りのリングを太くする | 外側の `--ring-width` |
| 顔を見せる位置を変える | 画像の `object-position` |
| 顔をさらに大きく見せる | 画像の `transform: scale(...)` |

外側の円がカンプと合っていて、顔の見え方だけ違うなら、外側の幅やpaddingまで一緒に動かしません。

### object-positionは写真を箱の中でどこに寄せるか

```css
.avatar__image {
  object-position: left center;
}
```

これは、画像データを画像要素の箱の中で左寄せ・上下中央にします。`cover` で左右が切れる状況なら、左側を残しやすくなります。

`left center` は `0% 50%` と表せます。微調整では `40% 50%` なども使えます。これは「元画像の横40%にいる人物を必ず中央にする」という意味ではなく、画像と箱の間の位置合わせです。実際の見え方を確認します。

一方、画像要素へ `transform: translateX(...)` を指定すると、要素の描画全体を横へ動かします。箱の中の写真の位置調整とは違い、空きや重なりが出ることがあります。

`transform` は周囲の通常の配置を押し直すものではありませんが、はみ出す範囲などへ影響します。写真の切り取り位置を直したいときは、まず `object-position` を見ます。[object-positionの仕様](https://drafts.csswg.org/css-images-3/#the-object-position)、[transformの描画への影響](https://www.w3.org/TR/css-transforms-1/#transform-rendering)。

### scaleで拡大しても外側の円は大きくならない

```css
.avatar__image {
  transform: scale(1.2);
}
```

`1.2` は、見た目を1.2倍にする指定です。初期状態では中心を基準に拡大します。外側の192pxや、内側の176pxというレイアウト上の寸法は作り直しません。

拡大した写真のはみ出しを、内側の `overflow: hidden` で隠します。この「指定した範囲の外を見せない」ことを、クリップすると呼びます。

## 12 画像の上に文字を重ねる

画像の中央に「関連サイト」と書きたいとします。

```html
<div class="banner">
  <img src="banner.png" alt="" width="258" height="94">
  <span class="banner__text">関連サイトのバナー予定</span>
</div>
```

```css
.banner {
  position: relative;
}

.banner img {
  display: block;
  width: 100%;
  height: auto;
}

.banner__text {
  position: absolute;
  inset: 0;
  display: grid;
  place-items: center;
}
```

この例では、次の順に考えます。

1. **画像を普通に表示する。** 画像の高さの分だけ、外側の `.banner` にも高さができる。
2. **文字を重ねる。** `position: absolute` で、文字を普通の縦並びから外す。画像の下に別の1行として置かない。
3. **重ねる基準を親にする。** `.banner` の `position: relative` が、文字の位置を決める基準を作る。
4. **文字用の箱を親いっぱいに広げる。** `inset: 0` は、この例では上下左右の端をすべて親の端へ合わせる。
5. **広がった箱の中で文字を中央へ置く。** `display: grid; place-items: center;` が担当する。

```text
親のbanner
├─ 画像                ← 親の高さを作る
└─ 画像全面に重なる箱  ← その箱の中で文字を中央へ置く
```

文字を絶対配置すると、その文字は親の高さを作りません。画像もなく、親にも高さや比率などの指定がなければ、文字を重ねる面の高さを確保できないことがあります。

画像がない仮のバナーなら、親に `aspect-ratio`、`min-height`、`padding` などで高さを用意します。文字量が増えたときは、絶対配置の文字が枠に収まっているかも確認します。

「通常フロー」は、要素が普通に並び、後の要素の位置に影響する流れのことです。ここでは画像がその流れに残り、文字は外れています。

## 13 absoluteなのにFlexで中央へ来る理由

ここは、12の書き方と違うコードを見かけたときの説明です。

```html
<div class="banner-flex">
  <img src="banner.png" alt="" width="258" height="94">
  <span class="banner-flex__text">バナー予定</span>
</div>
```

```css
.banner-flex {
  position: relative;
  display: inline-flex;
  justify-content: center;
  align-items: center;
}

.banner-flex img {
  display: block;
}

.banner-flex__text {
  position: absolute;
}
```

この例も、他の位置指定がなければ文字が中央へ来ます。

「絶対配置だからFlexとは一切関係ない」と覚えると、この結果が分からなくなります。次の2つを分けます。

- 文字は絶対配置なので、普通のFlexの項目として画像と横並びにはならない。
- でも、位置が未指定のときに使う仮の配置には、親のFlexの中央寄せが関係する。

`top`、`right`、`bottom`、`left` を書いていない場合、それらは通常 `auto` です。「全部0」ではありません。ブラウザは、未指定の位置を決めるための基準を使います。仕様ではこれを **static position** や **static-position rectangle** と呼びます。

この横並びの例では、左右方向の仮の位置に `justify-content` が関係します。上下方向では、仮の配置に使う子の `align-self: auto` が、親の `align-items` に従う仕組みが関係します。

そのため、文字は普通の「flex item」ではなく、**Flexの親の中にある、絶対配置の子**です。

### leftを足すと中央から動くのはなぜか

```css
.banner-flex__text {
  position: absolute;
  left: 0;
}
```

`left: 0` を足すと、左右方向に「親の左端へ置く」という具体的な条件ができます。

これは「leftの詳細度がjustify-contentより強い」という話ではありません。異なる指定が同じプロパティの勝敗を争っているのではなく、位置を計算する条件が変わっています。

| 書き方 | どう読むか |
|---|---|
| 親をFlexで中央寄せ、子に `absolute` だけ | 位置が未指定のときの仮の配置を使う |
| 子に `inset: 0`、子の中をGridで中央寄せ | 全面に広がる箱を明示し、その中で文字を中央にする |

どちらも成立する書き方です。元のノートでは、後から読んだ人が重ねる範囲を追いやすいよう、12の `inset: 0` の形を基本にしています。

確認先: [Flex内の絶対配置](https://drafts.csswg.org/css-flexbox-1/#abspos-items)、[align-self](https://drafts.csswg.org/css-align-3/#align-self-property)、[static position](https://drafts.csswg.org/css-position-3/#staticpos-rect)。

## 14 地図画像の上にピンを置く

地図が1枚の画像で、ピンだけを別のHTML要素で置く場合です。実際の地図サービスの緯度・経度を扱う話とは分けます。

```html
<div class="access-map">
  <img class="access-map__image" src="map.jpg" alt="店舗周辺の地図" width="800" height="600">
  <span class="access-map__pin">店舗</span>
</div>
```

```css
.access-map {
  position: relative;
  aspect-ratio: 4 / 3;
  overflow: hidden;
}

.access-map__image {
  display: block;
  width: 100%;
  height: 100%;
  object-fit: cover;
}

.access-map__pin {
  position: absolute;
  left: 50%;
  top: 50%;
  transform: translate(-50%, -50%);
}
```

この例では、親に比率を指定して高さの基準を作っています。元の短いコードにあった `height: 100%` の前提を補った形です。

`left: 50%; top: 50%;` は、親の横半分・縦半分の位置へ、ピンの左上を置きます。

そのままではピン自身が右下へずれるので、`translate(-50%, -50%)` で、今度は**ピン自身の幅と高さの半分**だけ戻します。これでピンの中心が親の中心と合います。

ピンの先端を地点へ合わせたいなら、中心を合わせるこの計算とは別に、ピンの形に応じた調整が必要です。

### ピンは地図の場所を理解していない

この50%は、あくまで親枠の中での位置です。画像内の店や交差点を自動追跡しているわけではありません。

スマホで枠の比率や `object-position` を変えると、見える地図の範囲が変わります。同じ50%の位置に、別の場所が見えることもあります。その場合は、ピンの位置も画像の見え方に合わせて確認します。

地図を自然な比率のまま表示し、同じ範囲を縮小するだけなら、画像内の割合に対応させた座標を保ちやすくなります。切り抜くと、その対応を改めて考える必要があります。

なお、`position: absolute` だけでは「左上に置く」という指定にはなりません。左上へ置きたいなら `top: 0; left: 0;` を書きます。

## 15 2倍画像は大きく表示するためではなく細かく表示するため

同じ800×450pxの表示領域に、800×450ピクセルの画像を使う場合と、1600×900ピクセルの画像を使う場合があります。

後者は、同じ場所を描くための画像データが細かくなります。高密度の画面での画質対策に使えます。表示領域が自動で2倍になるという意味ではありません。

### 必ず2で割ればよいわけではない

「基準表示サイズの正確な2倍で書き出した画像」と確認できたときは、次の計算で基準表示サイズが分かります。

```text
画像ファイルのピクセル寸法 ÷ 書き出し倍率 = 基準表示寸法
1600 × 900 ÷ 2 = 800 × 450
```

でも、400×400の画像がカンプでは210×210で表示されていたら、約1.9倍です。機械的に半分の200pxへ変えず、210pxで見せたい意図を先に確認します。

この場合、HTMLを `width="210" height="210"` とし、最終的な可変表示をCSSで調整できます。大事なのは、元画像と合う比率と、表示したい大きさを区別することです。

`1600 / 900` も `800 / 450` も16:9です。`aspect-ratio` の数字を半分にしても、表示は半分になりません。

### ブラウザへ1倍と2倍の候補を渡す

```html
<img
  class="density-photo"
  src="photo.jpg"
  srcset="photo@2x.jpg 2x"
  width="800"
  height="450"
  alt="山の風景"
>
```

```css
.density-photo {
  display: block;
  width: 100%;
  max-width: 800px;
  height: auto;
}
```

| 書いてあるもの | 役割 |
|---|---|
| `src="photo.jpg"` | 基本の画像。この例では1倍候補になり、srcsetを使わない環境の代わりにもなる |
| `srcset="photo@2x.jpg 2x"` | 同じ表示領域に対する2倍密度の画像候補 |
| HTMLの800×450 | 基準となる寸法と比率の情報 |
| CSSの `width` / `max-width` | 表示幅を決め、800pxを上限にする |
| `height: auto` | 選んだ画像の比率から表示の高さを決める |

どの候補を使うかは、画面の密度などを踏まえてブラウザが選びます。`@2x` というファイル名だけでは伝わりません。`srcset` に書いた `2x` が候補の関係を伝えます。

### 表示幅も大きく変わるならwとsizesを使う方法もある

同じ構図で、幅400、800、1600ピクセルの画像を用意した例です。

```html
<img
  class="density-photo"
  src="photo-800.jpg"
  srcset="photo-400.jpg 400w, photo-800.jpg 800w, photo-1600.jpg 1600w"
  sizes="(max-width: 800px) 100vw, 800px"
  width="800"
  height="450"
  alt="山の風景"
>
```

`400w` は「その画像ファイルの幅が400ピクセル」という候補情報です。`sizes` は、ブラウザが候補を選ぶために、画面上でどれくらいの幅で表示する予定かを伝えます。

この例の `100vw` は画面幅いっぱいという意味です。画面幅800px以下では画面幅いっぱい、それより広ければ800pxと伝えています。親の余白などで実際の表示幅が違う場合は、その配置に合わせて `sizes` も調整します。`sizes` 自体がCSSの表示幅を設定するわけではありません。

密度違いの `2x` と、ファイルの幅を示す `400w` は書き方の異なる方式です。同じ `srcset` の候補一覧の中で混ぜません。

**構図を変えるならpicture、同じ構図の細かさを選ぶならsrcset**、と分けると目的を追いやすくなります。

確認先: [HTML Standardのレスポンシブ画像](https://html.spec.whatwg.org/multipage/images.html#srcset-attribute)。

## 16 動画や角丸でも箱と中身を分ける

`video` でも、表示する箱と、その中に描かれる映像を分けて考えます。決めた領域に映像を収める例です。

```html
<div class="video-frame">
  <video class="video-frame__video" src="movie.mp4" controls width="640" height="360"></video>
</div>
```

```css
.video-frame {
  position: relative;
  aspect-ratio: 329 / 259;
  border-radius: 6px;
  overflow: hidden;
}

.video-frame__video {
  display: block;
  width: 100%;
  height: 100%;
  object-fit: contain;
}
```

`controls` は再生・停止などの操作を表示するための属性です。この例では映像全体を見せるため `contain` にしています。空きが出ても全体を見せたいのか、切ってでも埋めたいのかで選びます。

`border-radius: 6px` は親の角を丸くします。`overflow: hidden` は、その親からはみ出す子の表示を隠します。

単独の写真だけを角丸にするなら、画像自身に `border-radius` を付けられます。動画・ボタン・時間ラベルなどを一緒に丸い領域へ収めたいなら、共通の親で角丸と切り抜きを管理します。独自のボタンやラベルを重ねる場合の位置の基準が `position: relative` です。

### widthとheightを100%にするだけでは足りない

```css
.some-image {
  width: 100%;
  height: 100%;
}
```

親の幅と高さを使って画像要素の箱が決まったとしても、その形と元画像の形が違うことがあります。初期の収め方のまま両方へ引き伸ばすと、画像が変形し得ます。

- 元画像の割合を保って表示するなら、高さを `auto` にする。
- 決めた幅と高さの枠が必要なら、`object-fit` で収め方も決める。

「高さ100%」と「比率を保つ」は、同じ意味ではありません。

### 画像1枚だけのリンクはまず単純な形でよい

```html
<a class="media-link" href="details.html">
  <img src="photo.jpg" alt="詳しい紹介を見る" width="600" height="400">
</a>
```

```css
.media-link {
  display: block;
  aspect-ratio: 329 / 224;
  overflow: hidden;
}

.media-link > img {
  display: block;
  width: 100%;
  height: 100%;
  object-fit: cover;
}
```

リンクが枠とクリック範囲を担当し、画像がその中を埋めます。`>` は、このリンクの直下にある画像を選ぶ書き方です。

画像1枚だけなら、並べる機能のある `inline-flex` を追加せず、この形から始められます。画像やリンクが文字の行へ参加すると、ベースラインという文字の足元をそろえる基準に関係する隙間が出ることがあります。上の例では両方を `block` にして、行内配置による隙間を避けています。

`inline-flex` の中の画像に、必ず画像下の隙間ができるという意味ではありません。画像の下なのか、外側のリンクが入っている行の下なのかを区別します。

## 17 思った表示にならないときは実物を分けて確認する

ここまでの知識を全部覚えて診断する必要はありません。たとえば「PC画像には変わったけど、小さすぎる」と言えれば、見る対象を絞れます。

ブラウザのDevToolsで対象の `img` を選び、次の3つを分けます。

| 確認したいこと | 見る情報 |
|---|---|
| どの画像が選ばれたか | `currentSrc` |
| ブラウザが画像本来の寸法をどう扱っているか | `naturalWidth` / `naturalHeight` |
| 実際にどの大きさの箱で表示しているか | Computedの幅・高さ、ボックスモデル図など |

**画像ファイルの選択、表示する箱の大きさ、箱の中の写真の収め方は別です。**

### コンソールで見る場合

Elementsで対象の画像を選んだあと、コンソールで次を確認できます。Chrome DevToolsの `$0` は、Elementsで選択中の要素を指します。[Chromeのコンソール補助機能](https://developer.chrome.com/docs/devtools/console/utilities#recent)で確認できます。

```js
$0.currentSrc
$0.naturalWidth
$0.naturalHeight
getComputedStyle($0).width
getComputedStyle($0).height
```

`currentSrc` がPC画像なのに幅が375pxなら、画像選択よりCSSや寸法指定の方を調べます。

`naturalWidth` / `naturalHeight` は、密度を考慮した自然寸法です。たとえば1600×900のファイルを `2x` 候補として使うと、800×450相当として扱われることがあります。必ずファイルそのもののピクセル数と同じとは限りません。ファイルの画素数が知りたいときは、画像ファイル自体も確認します。[HTML Standardの自然寸法の計算](https://html.spec.whatwg.org/multipage/images.html#density-corrected-natural-width-and-height)。

### Attributes Styleとは何か

HTMLに次のように書いてあるとします。

```html
<img src="photo.jpg" width="800" height="450" alt="風景">
```

DevToolsのStylesには、`img[Attributes Style]` などの名前で、属性から作られた情報が見えることがあります。CSSファイルへ自分が同じ行を書いたという意味ではありません。

CSSによって表示幅が上書きされても、HTMLの寸法情報から得た比率が読み込み前の場所の確保に使われることがあります。

したがって、打ち消し線だけで「HTMLのwidthとheightは全部無意味になった」とは判断しません。最終的な幅・高さと、読み込み前に領域ができるかを分けて確認します。

ブラウザやバージョンによって、属性由来の情報の見せ方は違います。表示名を覚えるより、「自分のCSSか、HTML属性由来か」と「最終的に何pxか」を分けます。[web.devのAttributes Styleの説明](https://web.dev/articles/optimize-cls)。

## 18 どこまで確認できれば次へ進んでよいか

画像を1枚選び、次のように言えれば、その場の実装を進める足場になります。本文やAIを見ながらで構いません。

> この画像は全部見せたいので、幅を指定してheightをautoにした。
> 親を狭くすると画像も縮み、顔がつぶれずに全部見えている。

枠を使うなら、こんな確認です。

> このdivが正方形を作っている。
> imgはその大きさに合わせている。
> coverなので、写真の左右が切れている。

コードを何も見ずに書けることより、**触った指定と画面の変化を対応させられること**を先にします。

### 迷ったときに言葉にすること

- 困っている数値は、画像ファイルのピクセル数、HTMLの数値、画面での大きさ、縦横比のどれか。
- 写真を全部見せたいのか、枠を埋めたいのか。
- 今変えているのは、外側の並び、表示枠、中の写真のどれか。
- 読み込む前に、高さを用意できているか。
- 背景画像そのものが見えないなど、別の話が混ざっていないか。

これは毎回すべて埋めるチェック表ではありません。「どこが分からないか」を伝えるための見方です。

### たくさんある画像はどこから直すか

元のノートでは、ズレが広く波及しやすい場所から見る方針です。

1. カード一覧など、画像がたくさんある領域。
2. Gridなどで複数の画像を並べた領域。
3. ページ上部の大きなメイン画像。

画像の高さが後から変わると、その下にある文章や部品も動きます。枚数が多い場所やページ上部は、影響が広がりやすいので先に見る候補になります。

ただし、実際にページ上部の1枚が大きく動いていると分かったなら、そこから直せます。この順番自体を暗記するより、動いている範囲と影響の大きさで判断します。

元の「初期認識メモ」にあった「属性とCSSを併用する」「CLSのために先にサイズを予約する」は、このページの1・7に残しています。「確度: 高」という記録は元ノートでの評価であり、すべてのコードがあらゆる条件で成立するという意味ではありません。

## 19 元のノートと関連する説明の場所

この試し版は、貼ってもらった本文を説明し直すためのものです。各ノートの担当範囲は次のように分かれています。

| 知りたいこと | 元の説明 |
|---|---|
| 画像・動画の大きさ、切れ方、読み込み時のズレ | [10_メディア設計](./02_基礎/01_HTML・CSS/10_メディア設計（img・video・object-fit・aspect-ratio）.md) |
| imgと背景画像の使い分け、背景が見えない原因、SVGなどの形式 | [06_画像と背景](./02_基礎/01_HTML・CSS/06_画像と背景.md) |
| バナー、ロゴ、カード、iframeなど見た目の型から選ぶ | [13_デザインパターンとCSS固定判断](./02_基礎/01_HTML・CSS/13_デザインパターンとCSS固定判断.md) |

このページでは、背景画像の意味や画像形式の全解説まで広げず、元の10が扱う「配置と崩れ防止」を説明しています。

<details>
<summary>元の本文の各項目をどこで説明したか（抜けの確認用）</summary>

同じ内容を別の言い方で繰り返していた箇所は、対応する説明へまとめています。元の各見出しとの対応は次のとおりです。

| 元の項目 | この試し版 |
|---|---|
| 目的 | 冒頭・7 |
| この章の担当範囲（06・13との分担） | 19 |
| 学び直すときの入口 | 冒頭・18 |
| ルール | 1・3・7 |
| 実務での基本形 | 1・2 |
| 寸法・解像度・比率を分ける | 2・15 |
| DevToolsのAttributes Styleと最終表示を分ける | 17 |
| まず2択で決める（認知負荷を下げる） | 3・5 |
| 外・箱・中身で分解する | 6 |
| 画像自身と親のどちらにaspect-ratioを置くか | 5 |
| 画像ラッパーは必要な時だけ置く | 5 |
| 円形画像は外周・切り抜き枠・画像データを分ける | 11 |
| カンプと元画像からaspect-ratioの要否を判断する | 8 |
| pictureでPC/SP画像の比率が違う場合 | 9 |
| max-width: 100%は画像を親幅まで広げない | 9 |
| object-fit / aspect-ratio | 3・4 |
| 文字入り画像・ロゴ画像は自然縮小を先に見る | 10 |
| 同じSVGロゴでPC/SPの表示幅だけ変える | 10 |
| object-positionの表現と責務 | 11 |
| aspect-ratioが決めるもの / 決めないもの | 4 |
| 枠充填時の実装パターン | 5・16 |
| PC/SPで比率が違う画像の既定運用 | 9・15 |
| 地図画像とピン重ね | 14 |
| 画像の上に文字を重ねる基本形 | 12 |
| insetを省略してもFlex中央寄せになる場合 | 13 |
| 2倍書き出し画像の扱い | 15 |
| 比率情報が使える段階（実務判断） | 7 |
| 崩れやすい指定 | 5・16 |
| border-radiusとoverflow: hidden | 11・16 |
| 先に当てる優先順位（実務） | 18 |
| 初期認識メモ（確度） | 18 |
| 判断基準 | 18 |
| 一言でいうと | 2・6・7・18に分けて具体化 |

### 説明を正確にするために補った点

- 元の「object-positionは画像実体を動かさない」は、「画像要素の箱は動かさず、箱の中の画像データの位置を変える」と説明した。
- `transform` は周囲の通常の配置を押し直すわけではないため、「レイアウト副作用」を、描画のはみ出しや重なりと区別した。
- 地図の例に親の `aspect-ratio` を加え、画像の `height: 100%` の基準を明示した。
- 画像自身を比率枠にする例に `height: auto` を明示し、HTMLの高さ属性がある場合も読みやすくした。
- `naturalWidth` / `naturalHeight` は密度補正後の自然寸法であり、常にファイルの生のピクセル数と同じではないと補足した。
- リンクの `inline-flex` と画像下の隙間を一律に結び付けず、画像自身と外側の行内配置を分けた。

これらは試し版の中で説明を整えた点です。既存の本文や運用ルールは変更していません。

</details>
