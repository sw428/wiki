# 04_行ボックスとベースライン

## 目的

- 画像下隙間やラベルの縦ズレを、`margin` ではなく行内整列の問題として説明できるようにする。

## ルール

- 行内配置の基準箱は `line box`。
- 既定の縦揃えは baseline。
- `inline` 系の要素は、baseline/descender 由来の見かけの隙間が出ることがある。

## 例

```css
.media-link {
  display: inline-flex;
}

.media-link > img {
  display: block;
}
```

- `img` を `block` にしても、親が `inline-flex` なら外側inline文脈の影響が残る。
- 画像1枚リンクなら親ごと `display: block` へ切ると安定しやすい。

## 判断基準

1. 親の `display` は inline 系か
2. 行内基準（baseline）が残っているか
3. `vertical-align` 調整で済むか、文脈を block に切るか

## 関連ページ

- [02_インラインと行の仕組み](../../../01_学習本線/HTML・CSS/02_インラインと行の仕組み.md)
- [07_メディア設計（img・video・object-fit・aspect-ratio）](../../../01_学習本線/HTML・CSS/07_メディア設計（img・video・object-fit・aspect-ratio）.md)
