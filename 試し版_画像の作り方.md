# 写真を正方形の枠いっぱいに表示する

> 読み方を試すためのノートです。今回は、この作業一つだけを扱います。

## 作り方

### やること

横長や縦長の写真を、**最大300×300pxの正方形いっぱいに表示する。** 写真は引き伸ばして変形させず、入りきらない部分を切って見せます。

写真の一部が切れてもよい場合に使う形です。

### 1 HTMLの画像を置きたい場所に書く

横600・縦400ピクセルの `dog.jpg` を使う例です。実際に使うときは、画像のパス・説明・寸法を手元の画像に合わせます。

```html
<div class="card__media">
  <img
    class="card__image"
    src="dog.jpg"
    alt="芝生に座っている犬"
    width="600"
    height="400"
  >
</div>
```

**このHTMLのうち、CSSを書く相手は2つです。**

```text
div.card__media   ← 正方形を作る場所
└─ img.card__image ← その正方形の中へ入れる写真
```

`card__media` と `card__image` は、この例で付けたクラス名です。自分のコードで名前が違う場合は、HTMLとCSSで同じ名前を使います。

### 2 CSSファイルにこの2つを書く

そのHTMLから読み込んでいるCSSファイルに書きます。同じクラスの指定がすでにあれば、その場所を見直します。

```css
/* 外側のdiv：最大300pxの正方形を作る */
.card__media {
  width: 100%;
  max-width: 300px;
  aspect-ratio: 1;
  overflow: hidden;
}

/* 内側のimg：その正方形いっぱいに写真を入れる */
.card__image {
  display: block;
  width: 100%;
  height: 100%;
  object-fit: cover;
}
```

### 3 ブラウザでこの結果になったかを見る

- 写真の表示領域が正方形になっている。
- 正方形の中に、写真が入っていない帯状の空きがない。
- 犬の顔や体が、縦や横につぶれていない。

この例の横長写真では、**左右の一部が見えなくなるのが予定どおりの結果**です。元画像が正方形なら、切り抜きが起こらないこともあります。

置き場所の横幅が300px以上なら、表示領域は300×300pxです。置き場所が200pxに狭くなれば、200×200pxになります。ブラウザの幅を狭めたときも、正方形を保っているか見ます。

**ここまで確認できたら、今回の作業は一区切りです。** 理由を知りたいときは次へ、表示が違うときは「困ったとき」へ進みます。

## 基本

### なぜこの指定でできるのか

**外側の `.card__media` で正方形を作り、内側の `.card__image` を同じ大きさにして、写真を収めているからです。**

<details>
<summary>それぞれのCSSが何をしているか読む</summary>

まず、外側の `div` を見ます。

| `.card__media` に書いたもの | この例でしていること |
|---|---|
| `width: 100%` | このdivの置き場所の横幅に合わせる |
| `max-width: 300px` | ただし、300pxより大きくしない |
| `aspect-ratio: 1` | 横と縦を1:1にする。幅が300pxなら高さも300px |
| `overflow: hidden` | このdivからはみ出す表示を隠す |

次に、内側の `img` を見ます。

| `.card__image` に書いたもの | この例でしていること |
|---|---|
| `display: block` | 画像を文字の行に混ぜず、独立した箱として扱う |
| `width: 100%` | 画像要素の幅を、外側の正方形と同じにする |
| `height: 100%` | 画像要素の高さも、外側の正方形と同じにする |
| `object-fit: cover` | 写真の割合を保ちながら、その箱を埋める。入りきらない部分は切る |

同じ `width: 100%` でも、どこへ書くかで基準が違います。外側のdivは「その置き場所」、内側のimgは「外側のdiv」に合わせています。

この例では、外側の幅と `aspect-ratio` から高さが先に決まります。その高さがあるので、内側の `height: 100%` の基準が決まります。

**`object-fit: cover` だけでは正方形になりません。** 正方形の箱を用意する指定と、その中へ写真を収める指定を組み合わせています。

HTMLの `width="600" height="400"` は、元画像の幅と高さを伝える情報です。表示する箱はCSSで正方形にしています。HTMLの数値を300×300へ書き換える必要はありません。

さらに詳しい理由は、[前の試し版の「枠を画像自身に作るか親に作るか」](./試し版_画像と動画の表示をやさしく理解する.md#5-枠を画像自身に作るか親に作るか)で確認できます。

</details>

## 困ったとき

今起きていることに合う項目だけ見ます。

### 写真の下に空きがある

**内側の `.card__image` のCSSを見ます。**

`height: auto` が効いていると、画像が元の横長の割合で表示され、正方形の高さを埋めないことがあります。この作り方では `height: 100%` が効いているか確認します。

### 顔が切れるので全部見せたい

**内側の `.card__image` の1行を変えます。**

```css
object-fit: contain;
```

`cover` を `contain` に変更すると、写真全体が入る代わりに、正方形の中に空きが出ることがあります。「枠いっぱい」より「全体を見せる」を優先した結果です。

### 枠が正方形にならない

**外側の `.card__media` をブラウザのDevToolsで選びます。** `aspect-ratio: 1` が効いているか、別の `height` などが高さを決めていないかを見ます。

どの指定が効いているか分からなければ、該当するHTMLとCSSを出して「このcard__mediaを正方形にしたいが、縦長になる」のように聞けばよいです。関係する理由は、[前の試し版の「思った表示にならないとき」](./試し版_画像と動画の表示をやさしく理解する.md#17-思った表示にならないときは実物を分けて確認する)で確認できます。
