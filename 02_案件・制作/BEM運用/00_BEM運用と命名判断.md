# 00_BEM運用と命名判断

## 位置づけ（実務編レベル2）

このファイルは、案件制作でBEM命名を使うときの実務ルールを固定するノート。

- レベル1（基本概念）: `01_学習本線`
- レベル2（実務運用）: `02_案件・制作`

## このノートの目的

- クラス名だけ見て、役割を追える状態にする
- 詳細度競争を避け、修正コストを下げる
- SCSS分割（components/pages）と責務を揃える

## BEMの基本

- Block: 部品のまとまり
  - 例: `.contact-form`, `.faq`, `.card`
- Element: Blockの内部要素
  - 例: `.contact-form__field`, `.faq__question`
- Modifier: 状態・差分
  - 例: `.button--primary`, `.faq__item--open`

## 命名ルール（案件で固定）

1. `__` は基本1段まで（深いDOMをそのまま名前にしない）
2. Modifierは「状態差分」に限定する（見た目微調整の乱用を避ける）
3. 同じ役割の繰り返し要素は同じElement名を使う
4. 作品名・文言依存でElement名を分けない

## やらないこと

- `block__element__element` のような多段命名
- ページ都合の余白調整をBlock本体へ直接埋め込む
- IDセレクタやタグ連鎖で強引に勝たせる

## Blockを分ける判断

### 分ける（別Blockにする）

- 単体で再利用する可能性がある
- ページが変わっても意味が崩れない
- 独立した状態管理を持つ

### 分けない（Elementのままにする）

- 親Blockがないと意味が成立しない
- その文脈内でのみ使う部品

## `contact-card` と `contact-form` の扱い

今回の案件では次で固定する。

- `contact-card`: フォームを包む外箱（背景、角丸、内側余白）
- `contact-form`: 入力・送信の本体
- `contact-form__fields`: 入力項目群
- `contact-form__field`: 1項目（label + input）

外箱とフォーム本体を分けることで、見た目責務と機能責務が混ざりにくくなる。

## SCSS分割との対応

- `components`: 部品の見た目（Block側）
- `pages`: 配置・余白・並び（文脈側）

`pages` 側で `components` の核を上書きし続けると、BEMを使っても保守性は下がる。
命名だけでなく、責務配置もセットで運用する。

## 実務チェック（編集前）

1. そのクラスは Block / Element / Modifier のどれか
2. その調整は「核」か「文脈」か
3. 別ページ再利用の可能性があるか
4. 強いセレクタに頼らなくても書けるか
5. 3か月後に見ても意図を説明できるか

## まとめ

```txt
BEMは「命名ルール」だけではなく、
責務分離と保守性を維持するための実務ルールとして使う。
```
