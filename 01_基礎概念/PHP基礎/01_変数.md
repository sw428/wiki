# 01_変数

## 目的

PHPで値を一時的に入れておく場所として、変数を整理する。

## 最初に見る結論

変数は、値に名前をつけてあとから使えるようにするもの。

```php
$title = 'Hello';
echo $title;
```

## 見るポイント

- PHPの変数は `$` から始まる
- 文字列や数値などを入れられる
- `echo` で画面に出せる

## 自分の頭に残すこと

- PHPの変数は `$名前`。
- 値に名前をつけて再利用するために使う。
