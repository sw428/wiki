# 11_補助知識

## このファイルの役割

このファイルは、今すぐ主軸にしないが実装で必要になる知識を一時集約する保留枠。
優先度は `02〜05 / 07 / 09` より後ろに置き、必要になった時に個別追記する。

## いったんここにまとめる項目

- `this` / `bind` / `call` / `apply`
- クラス構文
- `localStorage`
- 日付処理
- 正規表現

## 保留追記枠

### this / bind / call / apply

### クラス構文

### localStorage

### 日付処理

### 正規表現

## 判断基準（暫定）

1. 現在の案件で直接必要か
2. 既存の関数設計やモジュール分割で代替できないか
3. 導入時に副作用（可読性低下・過剰抽象化）が増えないか
