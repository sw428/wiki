# 00_JS整理マップ

## このファイルの役割

このファイルは、JavaScriptメモ全体の入口。
各ファイルに何を書いたかと、どの順で読めばいいかをまとめる。

## このフォルダの見方

JavaScriptは、全部を一気に覚えるより、次の順で分けて見ると追いやすい。

- 土台
- データ
- DOM
- UI
- 非同期
- クロージャ
- 関数設計
- 基本文法
- モジュール分割
- 補助知識

## 認知アプローチ（知識に至る手順）

「文法暗記」ではなく、次の手順で判断力を作る。

1. 目的に戻る
まず「今何を作りたいか（例: ポートフォリオUI）」を固定する。
2. データの形を確認する
値は何型か、文字列かオブジェクトか、配列かを先に判定する。
3. 実行場所を確認する
ブラウザかNode.jsかを切り分け、使えるAPI前提を揃える。
4. 失敗点を最小化する
1行単位で `console.log` し、期待値と実値の差だけを見る。
5. 変更を局所化する
DOM取得・イベント・状態更新を分離し、どの層のミスか特定する。
6. 学習ログを残す
正解だけでなく「誤解した点」「修正理由」を短く残す。
7. 知識と再現を分ける
「知っている」ではなく「条件付きで再現できる」を到達基準にする。

## 重複を活かす読み方

同じ話題が別ファイルに出るのは、整理不足だけが理由ではない。
このフォルダでは、あえて角度を変えて重複させることがある。

- `01_JSの土台` は「言語の世界観」を掴むための重複
- `04_データと配列・オブジェクト` は「構造として扱う」ための重複
- `02_DOMとイベント` は「画面実装で再現する」ための重複

重複を見つけたら削る前に、次を確認する。

1. 何を理解させるための重複か（概念 / 構造 / 実装）
2. どの判断ミスを減らすための重複か
3. どの場面で再利用する前提か

## 没入モード（読むときの視点）

1. 世界観モード
「JSはなぜこう振る舞うか」を読む。

2. 実験モード
短いコードを実行し、結果のズレを観察する。

3. 制作モード
ポートフォリオ実装で使うなら、どの判断に効くかを紐づける。

## ファイル一覧

### [01_JSの土台](./01_JSの土台.md)

扱うもの:

- 値
- 型
- 演算子
- 式と文
- 変数
- 型変換
- 比較
- truthy / falsy
- NaN / Infinity

役割:
JSが何を扱う言語かを理解するための土台。

### [02_DOMとイベント](./02_DOMとイベント.md)

扱うもの:

- DOM
- querySelector
- querySelectorAll
- getElementById
- getAttribute
- classList
- textContent
- addEventListener
- event
- value
- preventDefault

役割:
HTMLをJSでどう触るかを理解するための土台。

### [03_UI実装パターン](./03_UI実装パターン.md)

扱うもの:

- ハンバーガーメニュー
- アコーディオン
- モーダル
- タブ
- フォームの基本確認

役割:
制作でよく出るUIの動きを、共通パターンで見る。

### [04\_データと配列・オブジェクト](./04_データと配列・オブジェクト.md)

扱うもの:

- オブジェクト
- プロパティ
- 配列
- JSON
- map / filter / reduce
- プリミティブ
- 参照とコピー

役割:
JSを「データの形を読む言語」として見るための整理。

### [05\_非同期と取得](./05_非同期と取得.md)

扱うもの:

- 非同期
- Promise
- fetch
- async / await
- response.json()

役割:
今はまだ値がない状態と、あとで届く値の扱いを整理する。

### [06\_スコープとクロージャ](./06_スコープとクロージャ.md)

扱うもの:

- スコープ
- 外側と内側
- クロージャ
- 参照の鎖
- 保持が解ける条件

役割:
なぜ値が保持されるのかを、参照のつながりとして理解する。

### [07\_関数設計](./07_関数設計.md)

扱うもの:

- 処理の分割
- 引数
- return
- 早期return
- 副作用の置き場所

役割:
DOM取得・状態更新・描画処理を分離し、関数単位で再利用しやすくする。

### [08\_基本文法メモ](./08_基本文法メモ.md)

扱うもの:

- 書き方の基本
- 変数名のルール
- 文字列
- テンプレートリテラル
- if / else if / else

役割:
小さい基本文法をまとめて、書くときの事故を減らす。

### [09\_モジュール分割](./09_モジュール分割.md)

扱うもの:

- import / export
- default export / named export
- main.js からの呼び出し
- UI単位の分割

役割:
実装量が増えたときに、JSファイルを責務ごとに分けて保守しやすくする。

### [10\_補助知識](./10_補助知識.md)

扱うもの:

- `this` / `bind` / `call` / `apply`
- クラス構文
- `localStorage`
- 日付処理
- 正規表現

役割:
主軸学習の外にある補助トピックを、必要時に参照できる形で一時集約する。

## 読む順番

### まず読む

- [01_JSの土台](./01_JSの土台.md)
- [04\_データと配列・オブジェクト](./04_データと配列・オブジェクト.md)

ここで「値・型・構造・参照」の土台を作る。

### 次に読む

- [02_DOMとイベント](./02_DOMとイベント.md)
- [03_UI実装パターン](./03_UI実装パターン.md)

ここで「制作でどう使うか」をつなげる。

### そのあと読む

- [05\_非同期と取得](./05_非同期と取得.md)
- [06\_スコープとクロージャ](./06_スコープとクロージャ.md)

ここは少し抽象度が上がるが、実務でも重要。

### 必要に応じて読む

- [07\_関数設計](./07_関数設計.md)
- [08\_基本文法メモ](./08_基本文法メモ.md)
- [09\_モジュール分割](./09_モジュール分割.md)
- [10\_補助知識](./10_補助知識.md)

## このフォルダの芯

このフォルダ全体の芯はこれ。

- JSは値を扱う
- JSはデータの形を読む
- DOMもオブジェクトとして読む
- UI実装はイベントと状態変更で作る
- 非同期は未来の値を扱う
- クロージャは参照の鎖で理解する
- 関数設計とモジュール分割で実装の責務を分ける
- 補助知識は必要時に切り出して使う

## 制作で最低限つながる順

制作目線で最低限つなげるなら、この順。

1. [01_JSの土台](./01_JSの土台.md)
2. [04\_データと配列・オブジェクト](./04_データと配列・オブジェクト.md)
3. [02_DOMとイベント](./02_DOMとイベント.md)
4. [03_UI実装パターン](./03_UI実装パターン.md)

この4本が先。

## 制作で使う最小セット（8割）

ポートフォリオ制作で先に押さえる実用チェック。
「わからない単語が出ても、この順に戻る」を前提にする。

### 1. 土台（型と構造）

- [ ] 型を見分けられる（`string` / `number` / `boolean` / `object` / `array` / `null` / `undefined`）
- [ ] オブジェクトと配列の違いを説明できる
- [ ] JSON文字列とオブジェクトの違いを説明できる

参照:
- [01_JSの土台](./01_JSの土台.md)
- [04_データと配列・オブジェクト](./04_データと配列・オブジェクト.md)

### 2. DOM操作

- [ ] `querySelector` / `querySelectorAll` で要素取得できる
- [ ] `textContent` / `classList` / `value` を使える
- [ ] `getAttribute`（初期値）と `プロパティ`（現在値）の違いを使い分けられる

参照:
- [02_DOMとイベント](./02_DOMとイベント.md)

### 3. イベントとUI

- [ ] `addEventListener` の第2引数に関数を渡せる
- [ ] `if` で状態分岐し、`classList.toggle` で開閉を作れる
- [ ] フォームで `preventDefault` と `value` を使える

参照:
- [02_DOMとイベント](./02_DOMとイベント.md)
- [03_UI実装パターン](./03_UI実装パターン.md)

### 4. 最低限の文法事故防止

- [ ] 大文字小文字ミスを潰せる
- [ ] 文字列クォート・セミコロン方針を揃えられる
- [ ] 全角スペース混入を疑える

参照:
- [08_基本文法メモ](./08_基本文法メモ.md)

### 5. 必要になったら追加で読む

- [ ] データ取得が必要なら `fetch` / `await`
- [ ] 値保持の挙動で詰まったらクロージャ
- [ ] 実装量が増えたら関数設計とモジュール分割へ進む
- [ ] 補助テーマが必要になった時だけ 10 を参照する

参照:
- [05_非同期と取得](./05_非同期と取得.md)
- [06_スコープとクロージャ](./06_スコープとクロージャ.md)
- [07_関数設計](./07_関数設計.md)
- [09_モジュール分割](./09_モジュール分割.md)
- [10_補助知識](./10_補助知識.md)

## ひとことで言うと

- 土台を作る
- データの形を読む
- DOMを触る
- UIを動かす

この順で見る。
