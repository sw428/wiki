# 09_モジュール分割

## このファイルの役割

このファイルは、JSファイルを責務ごとに分割するための保留枠。
いまは構成のみ先に置き、詳細は実装量が増えた段階で追記する。

## 先に固定する観点

- `import` / `export` をどう使い分けるか
- `default export` と `named export` の使い分け
- `main.js` でどこまで集約するか
- UI単位でどこまで分割するか

## 保留追記枠

### import / export の基本形

### default export と named export

### main.js からの呼び出し設計

### menu.js / accordion.js / form.js への分割基準

## 判断基準（暫定）

1. 1ファイルに複数UI責務が混在していないか
2. 変更時に影響範囲を追える分割になっているか
3. 分割のための分割になっていないか
