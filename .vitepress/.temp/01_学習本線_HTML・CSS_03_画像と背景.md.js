import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"03_画像と背景","description":"","frontmatter":{},"headers":[],"relativePath":"01_学習本線/HTML・CSS/03_画像と背景.md","filePath":"01_学習本線/HTML・CSS/03_画像と背景.md","lastUpdated":1779910689000}');
const _sfc_main = { name: "01_学習本線/HTML・CSS/03_画像と背景.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="_03-画像と背景" tabindex="-1">03_画像と背景 <a class="header-anchor" href="#_03-画像と背景" aria-label="Permalink to &quot;03_画像と背景&quot;">​</a></h1><h2 id="目的" tabindex="-1">目的 <a class="header-anchor" href="#目的" aria-label="Permalink to &quot;目的&quot;">​</a></h2><ul><li><code>&lt;img&gt;</code> と <code>background-image</code> の役割差を混同せずに判断できるようにする。</li><li>「背景が見えない」を、画像ファイルの問題ではなく <code>box</code> 面積の問題として切り分けられるようにする。</li><li>装飾画像の形式選定（<code>SVG</code> / <code>PNG</code>）を再利用しやすい判断軸で固定する。</li></ul><h2 id="この章の担当範囲-07との分担" tabindex="-1">この章の担当範囲（07との分担） <a class="header-anchor" href="#この章の担当範囲-07との分担" aria-label="Permalink to &quot;この章の担当範囲（07との分担）&quot;">​</a></h2><ul><li>この章（03）: <ul><li>意味画像と装飾背景の違い</li><li>背景が見えない時の原因切り分け</li><li>装飾画像の形式判断（<code>SVG</code> / <code>PNG</code>）</li></ul></li><li><a href="./07_メディア設計（img・video・object-fit・aspect-ratio）">07_メディア設計（img・video・object-fit・aspect-ratio）</a>: <ul><li><code>img</code> / <code>video</code> をレイアウト内で安定させる実装</li><li><code>width</code> / <code>height</code> 属性、<code>aspect-ratio</code>、<code>object-fit</code></li><li>CLS対策と崩れ防止パターン</li></ul></li></ul><h2 id="最初に押さえる結論" tabindex="-1">最初に押さえる結論 <a class="header-anchor" href="#最初に押さえる結論" aria-label="Permalink to &quot;最初に押さえる結論&quot;">​</a></h2><ol><li><code>&lt;img&gt;</code> は文書内の意味を持つ画像要素（<code>alt</code> を持つ）。</li><li><code>background-image</code> は既にある box への背景描画で、画像自体は box を作らない。</li><li>背景が見えない時は、まず「背景を描く box に面積があるか」を確認する。</li></ol><h2 id="背景画像と描画ボックス" tabindex="-1">背景画像と描画ボックス <a class="header-anchor" href="#背景画像と描画ボックス" aria-label="Permalink to &quot;背景画像と描画ボックス&quot;">​</a></h2><ul><li><code>background-image</code> は、既存要素の <code>box</code> に背景として描画される。</li><li>背景画像は DOM に意味を追加しない。</li><li>背景画像は要素サイズ（面積）を作らない。</li><li>そのため、要素側に <code>width</code> / <code>height</code> / <code>padding</code> / テキストなどの面積がないと見えにくい。</li></ul><table tabindex="0"><thead><tr><th>観点</th><th><code>&lt;img&gt;</code></th><th><code>background-image</code></th></tr></thead><tbody><tr><td>役割</td><td>意味画像（文書内容）</td><td>装飾背景（見た目）</td></tr><tr><td>DOM上の扱い</td><td>要素として存在する</td><td>新しい要素は増えない</td></tr><tr><td>セマンティクス</td><td>あり（<code>alt</code>）</td><td>なし</td></tr><tr><td>box面積への寄与</td><td>自身が描画対象になる</td><td>背景画像だけでは面積を作らない</td></tr></tbody></table><h2 id="背景が見えない時の確認順" tabindex="-1">背景が見えない時の確認順 <a class="header-anchor" href="#背景が見えない時の確認順" aria-label="Permalink to &quot;背景が見えない時の確認順&quot;">​</a></h2><ol><li>背景を描く要素に面積があるか（<code>width</code> / <code>height</code> / <code>padding</code> / テキスト）</li><li><code>display: inline</code> の空要素になっていないか</li><li>画像URLが正しいか</li><li><code>background-size</code> / <code>background-position</code> が意図どおりか</li></ol><p>補足:</p><ul><li><code>display: block</code> / <code>inline-block</code> は面積を作りやすいが、高さ指定も内容もないと高さ <code>0</code> になり得る。</li></ul><h2 id="装飾背景の描画ルール" tabindex="-1">装飾背景の描画ルール <a class="header-anchor" href="#装飾背景の描画ルール" aria-label="Permalink to &quot;装飾背景の描画ルール&quot;">​</a></h2><table tabindex="0"><thead><tr><th>プロパティ</th><th>意味</th></tr></thead><tbody><tr><td><code>background-size: cover</code></td><td>要素全体を覆う（比率維持、切り取りあり）</td></tr><tr><td><code>background-size: contain</code></td><td>要素内に収める（比率維持、余白あり）</td></tr><tr><td><code>background-position</code></td><td>切り取り時にどこを見せるかを決める</td></tr></tbody></table><h2 id="img-と-background-image-の選択基準" tabindex="-1"><code>&lt;img&gt;</code> と <code>background-image</code> の選択基準 <a class="header-anchor" href="#img-と-background-image-の選択基準" aria-label="Permalink to &quot;\`&lt;img&gt;\` と \`background-image\` の選択基準&quot;">​</a></h2><ul><li>意味画像として読ませる必要があるなら <code>&lt;img&gt;</code> を使う。</li><li>見た目の演出・装飾なら <code>background-image</code> を使う。</li><li>同じ見た目でも、意味の有無で選択を分ける。</li></ul><p>補足:</p><ul><li><code>object-fit</code> / <code>aspect-ratio</code> を使った「枠の安定化」は 07 側で扱う。</li></ul><h2 id="svg-と-png-の使い分け-装飾画像" tabindex="-1"><code>SVG</code> と <code>PNG</code> の使い分け（装飾画像） <a class="header-anchor" href="#svg-と-png-の使い分け-装飾画像" aria-label="Permalink to &quot;\`SVG\` と \`PNG\` の使い分け（装飾画像）&quot;">​</a></h2><ul><li><code>SVG</code> が向く: <ul><li>アイコン、ロゴ、単純図形、線画</li><li>拡大縮小しても劣化しにくい素材</li></ul></li><li><code>PNG</code> が向く: <ul><li>透過が必要なラスター画像</li><li>写真的でないが、ピクセル表現を固定したい素材</li></ul></li></ul><p>補足:</p><ul><li>写真は配信効率の観点で <code>WebP</code> / <code>AVIF</code> を優先検討し、<code>PNG</code> 常用は避ける。</li></ul><h2 id="判断基準" tabindex="-1">判断基準 <a class="header-anchor" href="#判断基準" aria-label="Permalink to &quot;判断基準&quot;">​</a></h2><ol><li>これは意味画像か、装飾背景か</li><li>装飾背景なら、背景を描く box に面積があるか</li><li>装飾画像の形式は <code>SVG</code> / <code>PNG</code> のどちらが適切か</li><li>実装安定化（<code>object-fit</code> / <code>aspect-ratio</code> / CLS）は 07 で設計する</li></ol></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("01_学習本線/HTML・CSS/03_画像と背景.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const _03______ = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  _03______ as default
};
